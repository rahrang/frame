import GlobalStyle from './GlobalStyle';
import {
  LIGHT_THEME_KEY,
  DARK_THEME_KEY,
  ThemeKeyType,
  getInitialTheme,
  getTheme,
  setTheme,
} from './theme';

export {
  GlobalStyle,
  LIGHT_THEME_KEY,
  DARK_THEME_KEY,
  ThemeKeyType,
  getInitialTheme,
  getTheme,
  setTheme,
};
